# frozen_string_literal: true

RSpec.describe LogAuditor do
  it 'has a version number' do
    expect(LogAuditor::VERSION).not_to be nil
  end

  xit 'does something useful' do
    expect(tr).to eq(true)
  end
end
